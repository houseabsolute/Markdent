# auto-generated shipit config file.
steps = FindVersion, ChangeVersion, CheckChangeLog, CheckVersionsMatch, DistTestExtra, Commit, Tag, MakeDist, DistClean

hg.tagpattern = %v
hg.push = 1

CheckChangeLog.files = Changes
